# 8.62 — Fedora 9 archive RPM

- **Package:** `ghostscript-8.62-3.fc9.x86_64.rpm`
- **Source:** Fedora archive
  (`https://archives.fedoraproject.org/pub/archive/fedora/linux/releases/9/Everything/x86_64/os/Packages/`)
- **Base image:** `centos:6` (glibc 2.12), `.repo` pointed at `vault.centos.org`
- **Method:** RPM payload extracted with `rpm2cpio | cpio`

## Dependency closure

The package ships `/usr/bin/gs` (needs `libgs.so.8`) and `libgs.so.8.62`.
`readelf -d libgs.so.8.62` is the same shape as 8.60 with one addition:
`libjasper.so.1`. Full list: `libjasper.so.1`, `libcupsimage.so.2`,
`libcups.so.2`, `libtiff.so.3`, `libjpeg.so.62`, `libpng12.so.0`, the krb5
sonames, `libz.so.1`, `libstdc++.so.6`, `libfontconfig.so.1`.

From CentOS 6 (`yum`): `libtiff libjpeg-turbo libpng cups-libs krb5-libs
fontconfig libstdc++ jasper-libs` (the `jasper-libs` package provides
`libjasper.so.1`), plus `binutils gdb`. No vault-only extras needed.

## Verification

- `gs --version` → `8.62`
- probe → `integertype` / `5000000000` ⇒ **64-bit** PostScript integers.
- `ldd /usr/bin/gs` reports no missing libraries.
